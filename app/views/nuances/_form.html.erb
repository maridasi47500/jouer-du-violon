<%= form_with(model: nuance) do |form| %>
  <% if nuance.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(nuance.errors.count, "error") %> prohibited this nuance from being saved:</h2>

      <ul>
        <% nuance.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :image, style: "display: block" %>
    <%= form.file_field :image %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
