<%= form_with(model: myfile) do |form| %>
  <% if myfile.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(myfile.errors.count, "error") %> prohibited this myfile from being saved:</h2>

      <ul>
        <% myfile.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :filename, style: "display: block" %>
    <%= form.text_field :filename %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
